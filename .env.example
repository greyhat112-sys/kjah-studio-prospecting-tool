# Copy this to .env.local and fill in real values
ADMIN_PASSWORD=your-secret-password
SESSION_SECRET=a-long-random-string

# From Vercel KV dashboard (Storage tab)
KV_REST_API_URL=
KV_REST_API_TOKEN=
